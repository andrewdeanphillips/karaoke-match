# Must match the host in the backend's SPOTIFY_REDIRECT_URI (127.0.0.1, not
# localhost) — otherwise the OAuth state cookie set on /auth/login won't be
# sent back when Spotify redirects to /callback, and login fails with
# "invalid or missing OAuth state".
VITE_API_BASE_URL=http://127.0.0.1:8080
