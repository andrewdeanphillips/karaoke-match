# KaraokeMatch Frontend

React + TypeScript frontend for KaraokeMatch, built with Vite.

## Development

```bash
cp .env.example .env
npm install
npm run dev
```

Requires the backend API running locally (see `../backend/.env.example` for
its default port). `VITE_API_BASE_URL` in `.env` must point to it.
